# Dockerfile-backend
FROM node:16-alpine

WORKDIR /backend

COPY backend/package.json backend/yarn.lock ./
RUN yarn install

COPY backend/src ./src
COPY backend/.env.example .env
RUN mkdir -p ./upload

CMD ["yarn", "start"]